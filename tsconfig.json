{
  "// note": "Type checking only - nothing is emitted. The project stays plain CommonJS JavaScript; this exists to catch the class of bug that shipped once already: calling an export a module does not have. noImplicitAny is off deliberately, so annotating every parameter is not a precondition for the useful checks.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "target": "es2022",
    "module": "commonjs",
    "skipLibCheck": true,
    "noImplicitAny": false,
    "strict": false,
    "resolveJsonModule": true,
    "esModuleInterop": true
  },
  "include": ["lib/**/*.js", "scripts/**/*.js"],
  "exclude": ["node_modules", "build", "lib/mclc"]
}
